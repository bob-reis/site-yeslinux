/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // tailwindcss: {}, // Disabled temporarily due to version incompatibility
    autoprefixer: {},
  },
}

module.exports = config